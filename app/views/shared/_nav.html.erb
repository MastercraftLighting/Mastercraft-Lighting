<div class="master-nav">
    <nav role="navigation" class="navbar navbar-inverse">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a href="/" class="navbar-brand">MC</a>
        </div>
        <!-- Collection of nav links and other content for toggling -->
        <div id="navbarCollapse" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <% if current_user && !admin? %>
                <li><a href="/users/#">Dashboard</a></li>
                <li><a href="/productions">Your Shows</a></li>
                <li><%= link_to "Libraries", libraries_path %></li>
                <li><a href="/productions/new" data-toggle="modal" data-target="#newModal">Create a new show</a></li>
                <% end %>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% if current_user %>
                    <% if admin? %>
                <li><%= link_to("Admin:users view", admins_path) %></li>
                    <% else %>
                <li><%= link_to("Update registration", edit_user_registration_path) %></li>

                    <% end %>
                <li><%=link_to("Logout", "/user/sign_out")%></li>
                <% else %>
                <li><%=link_to "Register", new_user_registration_path %></li>
                <li><%=link_to("Login", "/users/sign_in")%></li>
                <% end %>

            </ul>
        </div>
    </nav>
</div>
<%= render "productions/new_show_modal" %>

