require 'rails_helper'

feature "Libraries", :type => :feature, js: true do

  describe "Root Page" do
    # it "has a link to Libraries" do
    #   visit root_path
    #   expect(page).to have_selector("a","Libraries")
    # end
    # it "has a link that leads to Libraries Index" do
    #   # sign_in_as_designer
    #   visit root_path
    #   find(:css, "a[href='/libraries']").click
    #   expect(page).to have_current_path(libraries_path)
    # end
  end

end
